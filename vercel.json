{
    "buildCommand": "npm run build",
    "framework": "vite",
    "outputDirectory": "dist",
    "routes": [
        {
            "src": "^/assets/(.*)",
            "dest": "/assets/$1"
        },
        {
            "src": "^/(.*)",
            "dest": "/index.html"
        }
    ]
}